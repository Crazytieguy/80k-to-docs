node_modules/
.env
.env.local
*.local.json
/tmp/

# State file is committed (it's the durable progress log), but in-progress writes are not.
state/jobs.json.tmp

# OS noise
.DS_Store
.playwright-cli/
